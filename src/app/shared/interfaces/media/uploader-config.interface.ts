export interface IUploaderConfig {
  autoUpload?: boolean;
  showDropZone?: boolean;
  multiple?: boolean;
  showQueue?: boolean;
}

export interface IUploderOptions {
  id?: string;
  path?: string;
}
